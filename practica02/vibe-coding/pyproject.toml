[project]
name = "vibe-coding"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "maximo.alvarez", email = "maxandres90@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
vibe-coding = "vibe_coding:main"
vibe-api = "vibe_coding.api:start"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
